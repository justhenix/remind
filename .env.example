# remind environment configuration (Phase 1: NOT yet used)
# These placeholders describe the real backends that Phase 2 will wire up.
# In Phase 1 everything runs offline with in-memory + deterministic adapters.

# Supermemory Local HTTP endpoint (see https://supermemory.ai/docs/add-memories)
# SUPERMEMORY_URL=http://localhost:8000

# Ollama endpoint for local llm-bridge compression
# OLLAMA_URL=http://localhost:11434

# Local model used for compression
# OLLAMA_MODEL=llama3.2
